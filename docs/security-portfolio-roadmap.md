# Security Portfolio module contract

This repository owns `M04-transfer-verifier` at `implemented_development`: structural
provenance, freshness, approval, capability, and authority non-expansion checks. It does
not own authentication, semantic truth, incident probability, effects, or portfolio-wide
status.

The vendored [`security-portfolio-roadmap-public.yaml`](security-portfolio-roadmap-public.yaml)
is the digest-bound public projection. The profile copy is only a publication location.
The private Runtime Guard repository owns the canonical product roadmap. The local pin is
[`security-portfolio-roadmap-contract.json`](security-portfolio-roadmap-contract.json),
version `2026.08.02-r4-trajectory-containment`. Authority remains `none`.
